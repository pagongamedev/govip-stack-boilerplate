localhost:
  dialect: postgres
  datasource: host=localhost port=5432 user=postgres password=root dbname=govip sslmode=disable
  dir: migration
  table: migration

development:
  dialect: postgres
  datasource: host=3- port=5432 user=- password=- dbname=govip sslmode=- sslcert=../../../.secret/client.cert sslkey=../../../.secret/client.key
  dir: migration
  table: migration


seed:
  dialect: postgres
  datasource: host=localhost port=5432 user=postgres password=root dbname=govip sslmode=disable
  dir: seed
  table: seed

seed-integration:
  dialect: postgres
  datasource: host=localhost port=5432 user=postgres password=root dbname=govip sslmode=disable
  dir: seed-integration
  table: seed-integration